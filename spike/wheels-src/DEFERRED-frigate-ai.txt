# DEFERRED to the frigate-ai companion content-snap (plan-of-record: docs/m2-findings.md).
# Optional AI/ALPR/transcription + dev/NVIDIA-only entries excluded from the core snap.
# Lines below are VERBATIM from v0.17.2 docker/main/requirements-wheels.txt.
#
# NOTE (M2 Task-5 finding): Six entries from the original deferred list have been
# re-added to the core snap via the `validate-deps` snapcraft part. Frigate's
# module-level import graph (frigate.app → embeddings / data_processing / api) pulls
# these in unconditionally at startup, so the companion-snap deferral plan does NOT
# survive the import graph. See docs/m2-findings.md for the full import-chain trace.

## Re-added to core in M2 (module-level import graph - see docs/m2-findings.md)
pandas == 2.2.*
git+https://github.com/fbcotter/py3nvml#egg=py3nvml
transformers == 4.45.*
sherpa-onnx==1.12.*
librosa==0.11.*
soundfile==0.13.*

## Still deferred
mypy == 1.6.1
google-genai == 1.58.*
ollama == 0.6.*
openai == 1.65.*
faster-whisper==1.1.*
degirum == 0.16.*
memray == 1.15.*
